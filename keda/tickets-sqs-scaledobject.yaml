apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: tickets-sqs-scaler
  namespace: ticket-selling
spec:
  scaleTargetRef:
    name: tickets
  minReplicaCount: 0
  maxReplicaCount: 20
  pollingInterval: 10
  cooldownPeriod: 30
  triggers:
    - type: aws-sqs-queue
      metadata:
        queueURL: __SQS_TICKETS_QUEUE_URL__
        awsRegion: __AWS_REGION__
        queueLength: "5"
      authenticationRef:
        name: tickets-sqs-auth
---
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: tickets-sqs-auth
  namespace: ticket-selling
spec:
  podIdentity:
    provider: aws-eks
